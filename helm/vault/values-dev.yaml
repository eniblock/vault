server:
  debug: true

  dev:
    enabled: true

  standalone:
    enabled: false

  initSecrets:
    foo: bar

networkPolicy:
  useIngress: true
  ingress:
    from:
      - namespaceSelector:
          matchLabels:
            app.kubernetes.io/name: ingress

customInitScript: |
  vault write transit/keys/mykey type=ed25519
  echo $foo
